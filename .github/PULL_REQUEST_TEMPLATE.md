<!--
제목은 커밋 규약과 같게 씁니다.  feat: 포토이즘 지점 목록 수집 flow 추가 (BACKEND-90)

해당 없는 절은 통째로 지웁니다. 빈 채로 두면 리뷰어가 아직 안 쓴 것인지
없는 것인지 구분할 수 없습니다.

실측하지 않은 수치는 적지 않습니다. 확인하지 못한 것은 Before Merge 로 내립니다.
-->

## Ticket

BACKEND-

## Summary

<!-- 무엇을 어떤 범위로 했는지. 일부러 범위 밖으로 둔 것이 있으면 여기서 밝힙니다. -->

## Why

<!-- 판단의 근거. 고를 여지가 있었다면 버린 대안과 버린 이유를 적습니다.
     코드를 읽으면 알 수 있는 것 말고, 코드에 남지 않는 것을 적습니다. -->

## Pitfalls

<!-- 사이트나 API가 주는 함정과 대응. 다음 사람이 모르면 같은 데 빠질 것만 적습니다.
     없으면 절째로 지웁니다. -->

## Verification

<!-- 실제 실행 출력을 그대로 붙입니다. "확인했습니다"만 적지 않습니다.
     돌리지 않은 것은 여기 적지 말고 Before Merge 에 둡니다. -->

## Changed Files

| 파일 | 내용 |
| --- | --- |
|  |  |

## Before Merge

<!-- 머지 전에 누군가 해야 하는 것. 없으면 절째로 지웁니다.
     왜 지금 못 했는지와, 확인하지 않고 머지하면 무엇이 깨지는지를 함께 적습니다. -->

- [ ]

## Follow-up

<!-- 스택 PR, 이어지는 티켓, 이 PR에서 일부러 미룬 것. 없으면 절째로 지웁니다.
     스택이면 base가 main이 아니라는 것과 그 이유를 여기서 밝힙니다. -->
